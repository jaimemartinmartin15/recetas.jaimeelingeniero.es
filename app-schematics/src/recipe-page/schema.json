{
    "$schema": "http://json-schema.org/schema",
    "$id": "RecipePageSchema",
    "type": "object",
    "properties": {
        "name": {
            "type": "string",
            "description": "The name of the recipe",
            "$default": {
                "$source": "argv",
                "index": 0
            }
        }
    },
    "required": [
        "name"
    ]
}